# modx

Modx identifies anomalies in LLM models by looking inside the model!
So that you're safe not just before you deploy, but also after you deploy!

## Frontend

```bash
cd frontend
pnpm install
pnpm run dev
```
